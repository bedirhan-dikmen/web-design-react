# Store text with LF in the repository so the Linux server (Docker, shell,
# .env) always gets Unix line endings, whatever the editing machine uses.
* text=auto eol=lf

*.png binary
*.jpg binary
*.webp binary
*.ico binary
